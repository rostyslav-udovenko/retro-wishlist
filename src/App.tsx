import "./App.css";

function App() {
  return (
    <main className="app">
      <section className="app__placeholder" aria-labelledby="app-title">
        <p className="app__eyebrow">New project</p>
        <h1 id="app-title">Retro Wishlist</h1>
        <p>
          A colorful birthday wishlist is being built here.
        </p>
      </section>
    </main>
  );
}

export default App;
